# HubspotSettings SDK

HubSpot Settings API client, generated from the OpenAPI spec.

HubSpot Settings API — merged from the vendor's per-API OpenAPI documents.

Learn more about HubSpot Settings API at [hubapi.com](https://hubapi.com).

This is an unofficial SDK for the HubSpot Settings public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

Full documentation for this SDK: [https://voxgig-sdk.github.io/hubspot-settings-sdk/](https://voxgig-sdk.github.io/hubspot-settings-sdk/)

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

> **Features:** `debug`, `idempotency`, `metrics`, `paging`, `ratelimit`, `retry`, `test`, `timeout` — opt-in,
> inactive until switched on, and configured per client. See the Features
> section of any SDK README below for what each one does.

## Entities, not endpoints

This SDK exposes the API as **21 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new HubspotSettingsSDK()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = HubspotSettingsSDK.test({
  entity: {
    user_provisioning_collection_response_public_team_no_paging: {
      test01: { id: 'test01' },
    },
  },
})
const userprovisioningcollectionresponsepublicteamnopagings = await client.UserProvisioningCollectionResponsePublicTeamNoPaging().list()
// userprovisioningcollectionresponsepublicteamnopagings is an array of UserProvisioningCollectionResponsePublicTeamNoPaging entities, populated with mock data
// — call userprovisioningcollectionresponsepublicteamnopagings[0].data() for the record itself
console.log(userprovisioningcollectionresponsepublicteamnopagings)
```

### Python

```python
client = HubspotSettingsSDK.test()
userprovisioningcollectionresponsepublicteamnopagings = client.UserProvisioningCollectionResponsePublicTeamNoPaging().list()
print(userprovisioningcollectionresponsepublicteamnopagings)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = HubspotSettingsSDK::test([
    "entity" => ["userprovisioningcollectionresponsepublicteamnopaging" => ["test01" => []]],
]);
$userprovisioningcollectionresponsepublicteamnopagings = $client->UserProvisioningCollectionResponsePublicTeamNoPaging()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.UserProvisioningCollectionResponsePublicTeamNoPaging(nil).List(
    nil, nil,
)
```

### Lua

```lua
local client = sdk.test()
local results, err = client:UserProvisioningCollectionResponsePublicTeamNoPaging():list()
```

### JavaScript

```js
const client = HubspotSettingsSDK.test()
const userprovisioningcollectionresponsepublicteamnopagings = await client.UserProvisioningCollectionResponsePublicTeamNoPaging().list()
// userprovisioningcollectionresponsepublicteamnopagings is an array of entities, populated with mock data
// — call userprovisioningcollectionresponsepublicteamnopagings[0].data() for the record itself
console.log(userprovisioningcollectionresponsepublicteamnopagings)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/hubspot-settings` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-settings-sdk/tags) |
| Python | `voxgig-sdk-hubspot-settings` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-settings-sdk/tags) |
| PHP | `voxgig-sdk/hubspot-settings` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-settings-sdk/tags) |
| Golang | `github.com/voxgig-sdk/hubspot-settings-sdk/go` | `go get github.com/voxgig-sdk/hubspot-settings-sdk/go@latest` |
| Lua | `voxgig-sdk-hubspot-settings` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-settings-sdk/tags) |
| JavaScript | `@voxgig-sdk/hubspot-settings-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-settings-sdk/tags) |
| Go CLI | `github.com/voxgig-sdk/hubspot-settings-sdk/go-cli` | `go install github.com/voxgig-sdk/hubspot-settings-sdk/go-cli/cmd/hubspot-settings@latest` |
| Go MCP server | `github.com/voxgig-sdk/hubspot-settings-sdk/go-mcp` | `go get github.com/voxgig-sdk/hubspot-settings-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { HubspotSettingsSDK } from '@voxgig-sdk/hubspot-settings'

const client = new HubspotSettingsSDK({
  apikey: process.env.HUBSPOT_SETTINGS_APIKEY,
})


// Load a specific teamsteam (returns a TeamsTeam)
const teamsteam = await client.TeamsTeam().load({
  team_id: 'example_team_id',
})
console.log(teamsteam)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o hubspot-settings-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "hubspot-settings": {
      "command": "/abs/path/to/hubspot-settings-mcp"
    }
  }
}
```

## Entities

The API exposes 21 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Basic** | The Basic entity (remove). | `/settings/teams/2026-09/{teamId}/members/{userId}` |
| **ExchangeRate** | The ExchangeRate entity (create). | `/settings/currencies/2026-09/exchange-rates/update-visibility` |
| **MulticurrencyBatchResponseExchangeRate** | The MulticurrencyBatchResponseExchangeRate entity (create). | `/settings/currencies/2026-09/exchange-rates/batch/create` |
| **MulticurrencyCentralExchangeRatesInformation** | The MulticurrencyCentralExchangeRatesInformation entity (load). | `/settings/currencies/2026-09/central-fx-rates/information` |
| **MulticurrencyCollectionResponseCurrencyCodeInfoNoPaging** | The MulticurrencyCollectionResponseCurrencyCodeInfoNoPaging entity (list). | `/settings/currencies/2026-09/central-fx-rates/unsupported-currencies` |
| **MulticurrencyCollectionResponseExchangeRateForwardPaging** | The MulticurrencyCollectionResponseExchangeRateForwardPaging entity (list). | `/settings/currencies/2026-09/exchange-rates` |
| **MulticurrencyCollectionResponseExchangeRateNoPaging** | The MulticurrencyCollectionResponseExchangeRateNoPaging entity (list). | `/settings/currencies/2026-09/exchange-rates/current` |
| **MulticurrencyCompanyCurrency** | The MulticurrencyCompanyCurrency entity (load, update). | `/settings/currencies/2026-09/company-currency` |
| **MulticurrencyExchangeRate** | The MulticurrencyExchangeRate entity (create, load, update). | `/settings/currencies/2026-09/exchange-rates/{exchangeRateId}` |
| **TaxRate** | The TaxRate entity (list, load). | `/tax-rates/2026-09/tax-rates` |
| **TeamsBatchResponseTeamMember** | The TeamsBatchResponseTeamMember entity (create). | `/settings/teams/2026-09/{teamId}/members/batch` |
| **TeamsCollectionResponseTeamMemberResponseForwardPaging** | The TeamsCollectionResponseTeamMemberResponseForwardPaging entity (list). | `/settings/teams/2026-09/{teamId}/members` |
| **TeamsCollectionResponseTeamResponseForwardPaging** | The TeamsCollectionResponseTeamResponseForwardPaging entity (list). | `/settings/teams/2026-09` |
| **TeamsTeam** | The TeamsTeam entity (create, load, update). | `/settings/teams/2026-09/{teamId}` |
| **TeamsTeamMember** | The TeamsTeamMember entity (create). | `/settings/teams/2026-09/{teamId}/members` |
| **User** | The User entity (remove). | `/settings/users/2026-09/{userId}` |
| **UserProvisioningCollectionResponsePublicPermissionSetNo** | The UserProvisioningCollectionResponsePublicPermissionSetNo entity (list). | `/settings/users/2026-09/roles` |
| **UserProvisioningCollectionResponsePublicSeatNoPaging** | The UserProvisioningCollectionResponsePublicSeatNoPaging entity (list). | `/settings/users/2026-09/seats` |
| **UserProvisioningCollectionResponsePublicTeamNoPaging** | The UserProvisioningCollectionResponsePublicTeamNoPaging entity (list). | `/settings/users/2026-09/teams` |
| **UserProvisioningCollectionResponsePublicUserForwardPaging** | The UserProvisioningCollectionResponsePublicUserForwardPaging entity (list). | `/settings/users/2026-09` |
| **UserProvisioningPublicUser** | The UserProvisioningPublicUser entity (create, load, update). | `/settings/users/2026-09/{userId}` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from hubspotsettings_sdk import HubspotSettingsSDK

client = HubspotSettingsSDK({
    "apikey": os.environ.get("HUBSPOT_SETTINGS_APIKEY"),
})

```

### PHP

```php
<?php
require_once 'hubspotsettings_sdk.php';

$client = new HubspotSettingsSDK([
    "apikey" => getenv("HUBSPOT_SETTINGS_APIKEY"),
]);

```

### Golang

```go
import sdk "github.com/voxgig-sdk/hubspot-settings-sdk/go"

client := sdk.NewHubspotSettingsSDK(map[string]any{
    "apikey": os.Getenv("HUBSPOT_SETTINGS_APIKEY"),
})


// Load a specific teamsteam
teamsTeam, err := client.TeamsTeam(nil).Load(
    map[string]any{"team_id": "example_team_id"}, nil,
)
if err != nil {
    panic(err)
}
fmt.Println(teamsTeam)
```

### Lua

```lua
local sdk = require("hubspot-settings_sdk")

local client = sdk.new({
  apikey = os.getenv("HUBSPOT_SETTINGS_APIKEY"),
})

```

### JavaScript

```js
const { HubspotSettingsSDK } = require('@voxgig-sdk/hubspot-settings-js')

const client = new HubspotSettingsSDK({
  apikey: process.env.HUBSPOT_SETTINGS_APIKEY,
})


// Load a specific teamsteam (returns the entity)
const teamsteam = await client.TeamsTeam().load({
  team_id: 'example_team_id',
})
console.log(teamsteam)
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **DebugFeature** | Request/response capture ring buffer for debugging |
| **IdempotencyFeature** | Idempotency keys for safe retries of mutating operations |
| **MetricsFeature** | Statistics capture: per-operation counters and latency |
| **PagingFeature** | Pagination signals for list operations |
| **RatelimitFeature** | Client-side rate limiting via a token bucket |
| **RetryFeature** | Automatic retry of transient failures with exponential backoff |
| **TestFeature** | In-memory mock transport for testing without a live server |
| **TimeoutFeature** | Per-request timeout with transport abort |

Pass custom features via the `extend` option at construction time.

## Customizing this SDK

This repository contains its own generator (`.sdk/`), so the SDK is
customizable without forking any upstream tool:

- **The model** (`.sdk/model/`) declares everything this project owns:
  package names, versions, active features, per-target settings. It is
  written in [aontu](https://aontu.dev), a JSON-based
  specification language designed for building ontologies: easy to edit
  by hand, and files unify rather than override, so small declarations
  compose into one model. Regeneration re-reads it every time.
- **Templates** (`.sdk/tm/`) and **components** (`.sdk/src/cmp/`) are
  the two layers of generation, copied into this repo: templates are the
  literal per-language source, components generate the API-shaped parts.
- **Regeneration merges.** By default, newly generated content is
  three-way merged into existing files, so generator updates and local
  edits usually converge without manual conflict handling. A project can
  opt for plain overwrite instead.
- **Custom features and entire custom targets** arrive through sdkgen
  packages (`voxgig-sdkgen package add`), on the same rails as the
  bundled languages, and `voxgig-sdkgen doctor` reports any drift from
  what a resync would write.

How-to: [customize and propagate templates](https://github.com/voxgig/sdkgen/blob/main/docs/how-to/customize-and-propagate-templates.md).
The full story: [voxgig.com/sdk/custom](https://voxgig.com/sdk/custom).

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://api.hubapi.com](https://api.hubapi.com)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the HubSpot Settings API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
